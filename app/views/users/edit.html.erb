<h1>Edit <%= @user.name %></h1>

<%= bootstrap_form_for @user do |f| %>
  <%= render partial: "form", locals: {f: f} %>
  <br>
  <%= f.submit "Update User", class: "btn btn-primary btn-lg" %>
  <%= link_to "Cancel", @user, class: "btn btn-lg" %>
<% end %>
