en:
  activerecord:
    models:
      client_authorization:
        zero:  client authorizations
        one:   client authorization
        few:   client authorizations
        other: client authorizations
      consumer:
        zero:  consumers
        one:   consumer
        few:   consumers
        other: consumers
      upload:
        zero:  uploaded trip files
        one:   uploaded trip file
        few:   uploaded trip files
        other: uploaded trip files
      uploaded_trip:
        zero:  uploaded trips
        one:   uploaded trip
        few:   uploaded trips
        other: uploaded trips
      user:
        zero:  users
        one:   user
        few:   users
        other: users
