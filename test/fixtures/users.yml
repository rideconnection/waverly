user:
  name: Ride Connection User
  email: user@waverly.com
  # encrypted_password: <%= Devise.bcrypt(User, 'password') %>

admin:
  name: Ride Connection User
  email: admin@waverly.com
  # encrypted_password: <%= Devise.bcrypt(User, 'password') %>
